//Write a JavaScript program 
//to determine whether a given year is a leap year in the Gregorian calendar.  function myFunction() {
      function fromuser() {
        var year = document.getElementById("fourdigit").value;
        document.write(year);
    }
